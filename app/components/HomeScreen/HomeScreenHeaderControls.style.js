import {StyleSheet} from 'react-native';

const home_screen_header_controls_styles = StyleSheet.create({
  icon_android: {
    fontSize: GLOBAL_HEIGHT.HEADER_ICON,
    color: ICON_COLOR.ACTIVE.LIGHT
  },
  icon_ios: {
    fontSize: GLOBAL_HEIGHT.HEADER_ICON,
    color: PRIMARY_COLOR.MAIN
  }
});

export default home_screen_header_controls_styles;
